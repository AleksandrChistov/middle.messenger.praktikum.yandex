section.chat-card
    avatarComponent
    div.chat-card__content
        h3(class="chat-card__name")=authorName
        if textMessage
            p(class="chat-card__text")=textMessage
    div.chat-card__addons
        timeComponent
        if messageCount
            span(class="chat-card__count")=messageCount
